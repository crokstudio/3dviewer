{
  "name": "container-layer-viewer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "serve:dist": "node scripts/serve-dist.mjs",
    "verify:render": "node scripts/verify-render.mjs",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "three": "^0.184.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.42.4",
    "playwright": "^1.60.0",
    "vite": "^8.0.16",
    "wrangler": "^4.106.0"
  }
}
